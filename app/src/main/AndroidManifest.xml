<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.yashakabra05.codebotsapp">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="Vibez"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Codebotsapp">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB_7cSl_HKHJJJpOvFOpEzYRdnt8UweUGw" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".EnterEventSecondPage" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AskQuery" />
        <activity android:name=".ManangerQuery" />
        <activity android:name=".EmergencyTab" />
        <activity android:name=".MyParticularEvent" />
        <activity android:name=".MyEventsList" />
        <activity android:name=".Calendar" />
        <activity android:name=".Search" />
        <activity android:name=".Helpmain" />
        <activity android:name=".HomePage" />
        <activity android:name=".Favourite" />
        <activity android:name=".Feedback" />
        <activity android:name=".AboutUs" />
        <activity android:name=".Filter" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ProfileFirstPage" />
        <activity android:name=".EventInformation" />
        <activity android:name=".Create_event_sec_Activity" />
        <activity android:name=".CreateEventFirstActivity" />
        <activity android:name=".CreateActivity" />
        <activity
            android:name=".MainActivity"
            tools:ignore="DuplicateActivity" />
        <activity
            android:name=".CreateActivity"
            tools:ignore="DuplicateActivity" />
        <activity android:name=".EnterActivity" />
        <activity android:name=".LoginActivity">

        </activity>
    </application>

</manifest>